{
  "name": "tipspromenaden-functions",
  "private": true,
  "description": "Cloud Functions f\u00f6r Tipspromenaden: AI-genererade fr\u00e5gor + kreditk\u00f6p via Stripe",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "sync-shared": "node scripts/sync-shared.mjs",
    "prebuild": "npm run sync-shared",
    "build": "tsc",
    "typecheck": "npm run sync-shared && tsc --noEmit",
    "test": "npm run build && node --test \"lib/**/*.test.js\"",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth --project tipspromenaden-491207",
    "deploy": "firebase deploy --only functions --project tipspromenaden-491207",
    "test:emulator": "npm run build && firebase emulators:exec --only firestore --project demo-tipspromenaden \"node --test lib/credits.emulator-test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0",
    "stripe": "^22.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "~5.9.3"
  }
}
